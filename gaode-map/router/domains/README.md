# Domain Routers (Planned)

This directory is reserved for splitting `router/app.py` into domain routers:
- analysis
- history
- export
- road

Current release routes are fully mounted via domain routers in `router/app.py`.
